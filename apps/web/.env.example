NEXT_PUBLIC_API_URL="http://localhost:3001"

# Solo en producción SIN dominio propio (ver README > Deploy). Dominio del
# proyecto de Vercel de apps/api (ej. "https://fit-tracker-api.vercel.app"),
# usado por el rewrite en next.config.mjs para que /api/* se sirva desde el
# mismo origen que apps/web. Dejar sin definir en local.
# API_ORIGIN="https://fit-tracker-api.vercel.app"
